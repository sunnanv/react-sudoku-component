import {
    boardInitialState,
    timerInitialState
} from './initial_states';

const initialState = {
    board_reducer: boardInitialState,
    timer_reducer: timerInitialState
}

export default initialState;