import {
    boardInitialState,
    timerInitialState,
    helpInitialState
} from './initial_states';

const initialState = {
    board: boardInitialState,
    timer: timerInitialState,
    help: helpInitialState
};

export default initialState;